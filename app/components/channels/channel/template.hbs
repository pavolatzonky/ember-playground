<ChannelHeader
  @name={{@model.channel.name}}
  @description={{@model.channel.description}}
/>

<div class="py-4 flex-1 overflow-y-scroll channel-messages-list" role="list">
  <Messages @messages={{@model.messages}} />
  <Messages @messages={{@model.newMessages}} />
</div>

<footer class="pb-6 px-4 flex-none channel-footer">
  <MessageForm @onSendMessage={{this.onSendMessage}} />
  <InfoPanel @user={{hash nickname="Honza"}} />
</footer>