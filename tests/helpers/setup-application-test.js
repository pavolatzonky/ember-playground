import { setupApplicationTest } from 'ember-qunit';
// import setupFeatures from './setup-features';
// import setupResponsive from './setup-responsive';
// import setupSession from './setup-session';
// import setupScroll from './setup-scroll';
// import setupStore from './setup-store';
// import setupCaptcha from './setup-captcha';
// import setupRedirect from './setup-redirect';
// import setupMetrics from './setup-metrics';
// import setupSystem from './setup-system';
// import setupCookies from './setup-cookies';

export default function(hooks) {
  setupApplicationTest(hooks);
  // setupFeatures(hooks);
  // setupResponsive(hooks);
  // setupSystem(hooks);
  // setupSession(hooks);
  // setupScroll(hooks);
  // setupStore(hooks);
  // setupCaptcha(hooks);
  // setupRedirect(hooks);
  // setupMetrics(hooks);
  // setupCookies(hooks);
}
